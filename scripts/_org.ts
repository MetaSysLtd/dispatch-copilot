// Single source of truth for the default org id used by both seed and import.
// Override with DEFAULT_ORG_ID in .env if needed.
export const DEFAULT_ORG_ID =
  process.env.DEFAULT_ORG_ID ?? "28d09f64-02a1-4d73-be3d-11fd84045a91";
